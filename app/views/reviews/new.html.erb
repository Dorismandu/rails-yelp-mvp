<h1>New Review</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, as: :integer, min: '1', max: '5' %>
    <%= f.submit "Save", class: "btn btn-primary" %>
<% end %>
  